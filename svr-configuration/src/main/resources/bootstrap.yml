spring:
  application:
    name: svr-configurations

---
spring:
  profiles: dev
  cloud:
    config:
      server:
        git:
          #uri: http://192.168.1.220:10080/hill9868/ehr.config.git
          uri: http://192.168.1.220:10080/EHR/ehr.config.git
          basedir: /usr/local/ehr-config
        default-label: dev

---
spring:
  profiles: alpha
  cloud:
    config:
      server:
        git:
          uri: http://192.168.1.220:10080/EHR/ehr.config.git
          basedir: /usr/local/ehr-config
        default-label: dev

---
spring:
  profiles: test
  cloud:
    config:
      server:
        git:
          uri: http://192.168.1.220:10080/chenyongxing/ehr.config
          basedir: /usr/local/ehr-config
        default-label: dev

---
spring:
  profiles: prod
  cloud:
    config:
      server:
        git:
          uri: http://11.1.2.8:3000/ehr_admin/ehr.config.git
          basedir: /usr/local/ehr-config
        default-label: master

---
spring:
  profiles: prod_fz
  cloud:
    config:
      server:
        git:
          uri: http://11.1.2.1:3000/jkzl/ehr.config.git
          basedir: /usr/local/ehr-config
        default-label: master

---
spring:
  profiles: prod_sr
  cloud:
    config:
      server:
        git:
          uri: http://100.100.1.153:3000/jkzl/ehr.config.git
          basedir: /usr/local/ehr-config
        default-label: master

---
##不想用远程的git 可以在环境变量配置native 开启本地配置
spring:
  profiles:
    active: native

##git配置
  cloud:
    config:
      server:
        native:
          search-locations: file:D:\code\ehr.config  ##本地的git配置路径
