spring:
  application:
    name: ag-ehr-platform

  cloud:
    config:
      username: user
      password: configuration

      uri: http://localhost:1221
      label: dev

encrypt:
  failOnError: false