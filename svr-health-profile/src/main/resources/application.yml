server:
  port: ${svr-health-profile.server.port}

management:
  port: ${server.port}

info:
  app:
    name: HealthProfileService
    description: EHR platform health profile service.
    version: 1.0.0

---
spring:
  profiles: dev

---
spring:
  profiles: test

---
spring:
  profiles: prod
