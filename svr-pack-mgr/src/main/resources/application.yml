server:
  port: ${svr-pack-mgr.server.port}

management:
  port: ${server.port}

info:
  app:
    name: JsonPackageService
    description: EHR platform package service.
    version: 1.1.0

---
spring:
  profiles: dev

---
spring:
  profiles: test

---
spring:
  profiles: prod
