server:
  port: ${svr-adaption.server.port}

management:
  port: ${server.port}

info:
  app:
    name: AdaptionService
    description: EHR platform adaption service.
    version: 1.0.0

---
spring:
  profiles: dev

---
spring:
  profiles: test

---
spring:
  profiles: prod


