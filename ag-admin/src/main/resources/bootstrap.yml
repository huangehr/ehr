spring:
  application:
    name: ag-admin
  cloud:
    config:
      username: user
      password: configuration

---
spring:
  profiles: dev
  cloud:
    config:
      uri: ${spring.config.uri:http://172.19.107.73:1221}
      label: ${spring.config.lable:dev}

---
spring:
  profiles: prod
  cloud:
    config:
      uri: ${spring.config.uri}
      label: ${spring.config.lable}