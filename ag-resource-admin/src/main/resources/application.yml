server:
  port: 10200
  compression:
    enabled: true
    min-response-size: 1024
    mime-types: application/xml,application/json


management:
  port: ${server.port}

info:
  app:
    name: AgResourceAdminService
    description:  ehr resource app service.
    version: 1.0.0
  component: Zuul Server

hystrix:
  command:
    default:
      execution:
#        timeout:
#          enabled: false
        isolation:
          thread:
            timeoutInMilliseconds: 30000
ribbon:
  ReadTimeout: 30000
  ConnectTimeout: 6000

---
spring:
  profiles: dev

---
spring:
  profiles: test

---
spring:
  profiles: prod

