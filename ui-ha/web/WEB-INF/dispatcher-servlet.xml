<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <context:component-scan base-package="com.yihu.ha">
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Service"/>
    </context:component-scan>

    <mvc:annotation-driven/>

    <!-- 允许访问指定路径下的静态资源 -->
    <mvc:resources location="static-dev/" mapping="/static-dev/**"/>
    <mvc:resources location="develop/" mapping="/develop/**" />
    <mvc:resources location="WEB-INF/" mapping="/WEB-INF/**" />

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/ehr/jsp/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!--解决使用@Configuration注解时，单元测试产生ServletContext异常-->
    <bean class="com.yihu.ha.api.SwaggerConfig"/>

    <bean id="tilesViewResolver" class="org.springframework.web.servlet.view.tiles3.TilesViewResolver" p:order="1">
    </bean>

    <bean id="tilesConfigurer" class="org.springframework.web.servlet.view.tiles3.TilesConfigurer">
        <property name="definitions">
            <list>
                <value>/WEB-INF/ehr/commons/layout/layout.xml</value>
                <value>/WEB-INF/ehr/commons/layout/browserLayout.xml</value>
            </list>
        </property>
    </bean>
    <!--api接口系统请求参数，拦截器（目前只做了非空判断)
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/rest" />
            <bean class="com.yihu.ha.interceptor.RestUrlInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>-->
</beans>