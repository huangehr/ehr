server:
  port: ${ag-admin.server.port}

management:
  port: ${server.port}

info:
  app:
    name: AgAdminService
    description: EHR platform json app service.
    version: 1.0.0
  component: Zuul Server

---
spring:
  profiles: dev

---
spring:
  profiles: alpha

---
spring:
  profiles: test

---
spring:
  profiles: prod

---
spring:
  profiles: prod_fz

---
spring:
  profiles: prod_sr

---
spring:
  profiles: prod_fzzy