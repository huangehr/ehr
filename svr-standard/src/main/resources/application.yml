
server:
     port: 10040

#security:
#  user:
#    password: standard

info:
 app:
   name: StandardService
   description: EHR platform standard service.
   version: 1.0.0

ehr:
 commons:
   repeat:
     code: 代码已存在!
common:
 query:
   not:
     found: 该{0}不存在：{1}
   miss:
     parameter:  参数不能为空：{0}！
   notfound:
     entity: 没有找到{0}实体：{}
std:
 version:
   invalid:
     stage: 该版本不处于编辑状态！

---
spring:
  profiles: dev
eureka:
  instance:
    ip-address: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl.defaultZone: ${vcap.services.eureka-service.credentials.uri:http://user:eureka@127.0.0.1:8761}/eureka/

---
spring:
  profiles: sand-home-dev

eureka:
  instance:
    ip-address: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl.defaultZone: ${vcap.services.eureka-service.credentials.uri:http://user:eureka@127.0.0.1:8761}/eureka/

---
spring:
  profiles: test
eureka:
  instance:
    ip-address: 192.168.131.151
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl.defaultZone: ${vcap.services.eureka-service.credentials.uri:http://user:eureka@192.168.131.151:8761}/eureka/


---
spring:
  profiles: prod

eureka:
  instance:
    ip-address: 11.1.2.21
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl.defaultZone: ${vcap.services.eureka-service.credentials.uri:http://user:eureka@11.1.2.21:8761}/eureka/
